tout les fichier .txt ici précent sont des fichier créer par fr-simplecode (si un message d'erreur se produit au première exécution de code sur des fichiers, copier les noms des fichiers dans des fichiers vides).

Le contenu des fichier est ici à titre d'exemple, il se génère normalement automatiquement.